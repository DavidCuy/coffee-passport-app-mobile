<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Requerido por `geolocator` (feature `scan`) para capturar
         lat/lng al momento de enviar POST /scan — ver
         lib/features/scan/data/datasources/geolocation_datasource.dart -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Requerido en builds release (debug ya lo trae vía
         android/app/src/debug/AndroidManifest.xml) para consumir
         coffee-passport-backend vía HTTP (core/network/api_client.dart). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="coffee_passport_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- Requerido por `google_maps_flutter` (feature `shop_directory`,
             vista de mapa — ver
             lib/features/shop_directory/presentation/widgets/shop_map_view.dart).
             ⚠️ Placeholder: sin una API key real de Google Maps (Google Cloud
             Console, restringida a este `applicationId` + SHA-1), el mapa
             compila y corre pero se ve en blanco / con la marca de agua
             "for development purposes only". Reemplazar antes de un build
             real de QA/producción — no es un secreto de servidor, pero sí
             debe restringirse por paquete+huella para no quedar abierta. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_GOOGLE_MAPS_API_KEY"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
